# 01_chatbot_assurance / AssurBot — Chatbot RAG Assurance
# Streamlit + Scikit-learn (retrieval TF-IDF) + génération via API compatible OpenAI (Groq)
streamlit>=1.28.0
scikit-learn>=1.3.0
numpy>=1.24.0
openai>=1.30.0
