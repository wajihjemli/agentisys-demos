# 03_assurocr / AssurOCR — OCR + Structuration de Constats
# Streamlit + Tesseract OCR (pytesseract) + structuration via API compatible OpenAI (Groq)
streamlit>=1.28.0
pytesseract>=0.3.10
Pillow>=10.0.0
openai>=1.30.0
